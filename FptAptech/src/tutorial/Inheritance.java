package tutorial;

public class Inheritance {
	// kế thừa là kế thừa toàn phần,kế thừa toàn bộ chứ ko phải kế thừa 1 phần,thằng cha có j thì thằng con có hết.
	// có final thì ko thể overwrite được, ko thay đổi giá trị của nó được. ko thể trỏ đến nó được.
	// ví dụ dùng final: tên cơ sở dữ liệu.
	// chỉ có thể extends 1 class. nhưng có thể implement nhiều interface.
	// Đa hình: là cùng 1 hành vi nhưng trên các class khác nhau là khác nhau.
	// kế thừa có 2 loại: overwriting và overloading.
	// overwriting : định nghĩa lại phương thức của lớp cha.
	// overloading: dùng lại phương thức của lớp cha, ko thay đổi j.
	
	/*interface:
	 * Đa kế thừa;
	 * Định nghĩa các hành vi sẽ được triển khai trong chương trình;
	 * 1 interface có thể extends được nhiều interface khác;
	 * */
	/*interface vs abstract class:
	 * 
	 *
	 * */
}
